package firstpackage;

public class StringDigitDemo {
	public static void main(String[] args) {
		int num = 100;
                system.out.println(num);
                system.out.println("num");
		
		String str = "i";
		str += " am";
		str += " happy";
		System.out.println(str);
		
		String str1 = "DeepLearning";
                system.out.println(str1);
    	}
}

/*A Demonstration of String Variables, one of the commonly used Reference type Variables, and Some Examples codes
-<변수 타입> <변수 명> = <"변수 값">
ex)String str = "i am"

-String 변수는 클래스임과 동시에 변수이기도 하다.
-숫자와 문자가 같이 있으면 문자가 우선시된다.*/

//참조)교재 68p - 표 2-3

//어떤 숫자인지를 확인해야 한다면
------------------------------------------------
int num = 100;
system.out.println("num:"+num);
int num1 = 200;
system.out.println("num1:"+num1);
------------------------------------------------

//숫자를 일일히 한줄씩 확인하기 귀찮다면
------------------------------------------------
int num = 100;
system.out.print("num:"+num+"   ");
int num1 = 200;
system.out.println("num1:"+num1);
------------------------------------------------

------------------------------------------------
String str = "100";
int num = 200;
String res = str + num;
System.out.println(res);
------------------------------------------------

------------------------------------------------
String str = "100";
char ch = 'a';
int num = 200;
String res = str + num;
res += ch;
(res = res + ch;)
System.out.println(res);
------------------------------------------------
