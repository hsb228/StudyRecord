/*<Constructor>

-메소드 뒤에 ()로 표시한다.
-클래스를 가장한 메소드로, 기본적으로는 return이 존재하지 않는다.
(요즘 자바에서는 return을 받을 수 있다.)

-생성자는 반드시 모든 클래스에 하나 이상은 존재해야 한다.
-기본 생성자는 대부분의 경우 생략되어있다. 그리고 객체가 생성되고 딱 한번만 실행된다.

-생성자 안에 값을 넣으면 더 이상 자동 생략되는 디폴트로 취급되지 않는다.
-같은 메소드에 생성자가 다른 경우, 값이 동일해도 엄연히 다른 객체가 나온다. 

-참고)p.217~218*/
